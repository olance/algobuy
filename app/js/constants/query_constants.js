// Dispatched when the search query has changed
export const QUERY_CHANGED = Symbol('QueryChanged');

// Dispatched when the query has been cleared
export const QUERY_CLEARED = Symbol('QueryCleared');
