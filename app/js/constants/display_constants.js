// Action type sent to display a product
export const DISPLAY_PRODUCT = Symbol('DisplayProduct');

// Action type sent to display the results of a suggested search
export const DISPLAY_SEARCH = Symbol('DisplaySearch');

// Action type sent when the result of the suggested search have arrived
export const DISPLAY_SEARCH_RESULTS = Symbol('DisplaySearchResults');
